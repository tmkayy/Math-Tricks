#include <iostream>
#include "GenerateMatrix.h";
#include "Operation.h"

int main()
{
    int cols = 0;
    int rows = 0;
    std::cout << "Input size of the board (rows and columns):";
    std::cin >> rows >> cols;
    Operation** matrix = new Operation* [rows];
}

